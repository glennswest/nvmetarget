from setuptools import find_packages, setup

setup(
    name='nvmetarget',
    packages=find_packages(),
    description='Library to create nvmetarget from disk images on a linux host',
    author='glennswest@neuralcloudcomputing.com',
    install_requires=[],
)

